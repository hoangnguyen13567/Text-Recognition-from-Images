# Text-Recognition-from-Images
An image-based text recognition system where EasyOCR is used to detect text regions in input images. A CNN built from scratch extracts high-level visual features, and a Transformer-based decoder generates character-level text sequences.
